extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/users/register") Register
  if (user)
    p You are currently logged in as #{user.email}
    a(href="/logout") Logout
