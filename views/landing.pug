extends layout

block content
  .welcomeBox
    h1 Welcome user!

  .listBox
    each val in lists
      h1= val.title
      .todo
        each todo in val.todos
          .description= todo.description
